data/*.db
data/*.db-wal
data/*.db-shm
data/.session_secret
data/.mpesa_encryption_key
data/uploads/
node_modules/
.env
*.tmp
test/frontend-integration.combined.js

# Scratch space for test/run-all.sh and test/run-frontend.sh — the test
# database, its WAL/SHM sidecars, seed output, and server logs for each
# suite. Never the real application database (see rhinocash-backend/data/
# above); this directory only ever holds throwaway test-run artifacts.
test/tmp/
